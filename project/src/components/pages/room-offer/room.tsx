// import { Offer } from '../../../types/offer';
// import { City, Points } from '../../../types/types';
// import RoomOfferList from './room-offer';

type OfferListProps = {
  // offers: Offer[];
  // comments: Comment[];
  // city: City;
  // points: Points;
};

function Room(props: OfferListProps): JSX.Element {
  // const { offers, comments, city, points } = props;
  return (
    // eslint-disable-next-line react/jsx-no-useless-fragment
    <>
      {/* <RoomOfferList offers={offers} comments={comments} city={city} points={points} /> */}
    </>
  );
}

export default Room;
